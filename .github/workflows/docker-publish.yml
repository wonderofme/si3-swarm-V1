name: Build and Publish Docker image to GHCR
# Workflow builds and pushes to ghcr.io/wonderofme/kaia-swarm

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        id: build
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/kaia-swarm:v213
            ghcr.io/${{ github.repository_owner }}/kaia-swarm:latest
            ghcr.io/${{ github.repository_owner }}/kaia-swarm:${{ github.sha }}


